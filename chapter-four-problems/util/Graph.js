class Graph {
  constructor () {
    this.vertices = [];
    this.edge = [];
    this.numOfEdges = 0;
  };

  add (vertex) {
    this.vertices.push(vertex);
    this.edge
  };
};

module.exports = Graph;